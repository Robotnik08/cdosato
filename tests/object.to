make object obj = {}

set obj = {"a": 1, "b": 2, "c": 3, "d": 4, "e": 5}

do sayln (obj)

do sayln (obj->"a")

set obj->"b" *= 2
set obj->c *= 3

do sayln (obj)

do sayln (obj->"b")

set obj += {"f": 6, "g": 7, "h": 8, "i": 9, "j": 10}

do sayln (obj)

set obj -= "h"

do sayln (obj)
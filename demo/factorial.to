define long factorial (long n) {
    make result = 1
    set result *= i for i in range(1, n + 1)
    return result
}

do sayln(factorial(0)) // 1
do sayln(factorial(3)) // 6
do sayln(factorial(5)) // 120
do sayln(factorial(10)) // 3628800
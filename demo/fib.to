// Simple recursive Fibonacci function
define long fib (long n) {
    make a, b = 0, 1
    set a, b = b, a + b for (range(n - 1) => _)
    return b
}

do sayln(fib(0)) // 1
do sayln(fib(3)) // 3
do sayln(fib(5)) // 8
do sayln(fib(10)) // 89
do sayln(fib(15)) // 987